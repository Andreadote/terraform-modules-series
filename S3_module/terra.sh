#!/bin/bash

# we are using this to call our fuction file # bash terraform.sh 
#from here, and we could either call 
#each section on at atime or all or remove a section of the fuction..   
#this makes our code reusable (remote calling )
#( this is the script we are using (bash terra.sh))

source terraform.sh #<<<<------  (by writing the absolute path you can call ur script from anyway )

evironment
tfsec
directory
plannning
testing
#Apply